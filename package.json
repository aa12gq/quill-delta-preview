{
  "name": "quill-delta-preview",
  "displayName": "Quill Delta Preview",
  "description": "Preview Quill Delta JSON files in VSCode",
  "version": "1.0.2",
  "publisher": "aa12gq",
  "icon": "icon.png",
  "repository": {
    "type": "git",
    "url": "https://github.com/aa12gq/quill-delta-preview.git"
  },
  "engines": {
    "vscode": "^1.74.0"
  },
  "categories": [
    "Other"
  ],
  "keywords": [
    "quill",
    "delta",
    "preview",
    "json",
    "rich-text"
  ],
  "activationEvents": [
    "onCommand:quillDeltaPreview.openPreview"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "quillDeltaPreview.openPreview",
        "title": "Open Quill Delta Preview",
        "category": "Quill Delta"
      }
    ],
    "menus": {
      "explorer/context": [
        {
          "when": "resourceExtname == .json",
          "command": "quillDeltaPreview.openPreviewFromExplorer",
          "group": "navigation"
        }
      ],
      "editor/context": [
        {
          "when": "resourceExtname == .json",
          "command": "quillDeltaPreview.openPreview",
          "group": "navigation"
        }
      ]
    },
    "keybindings": [
      {
        "command": "quillDeltaPreview.openPreview",
        "key": "ctrl+shift+q",
        "mac": "cmd+shift+q",
        "when": "resourceExtname == .json"
      }
    ]
  },
  "scripts": {
    "vscode:prepublish": "npm run package",
    "compile": "node build.js",
    "watch": "node build.js --watch",
    "package": "node build.js --production",
    "pretest": "npm run compile && npm run lint",
    "lint": "eslint src --ext ts",
    "test": "node ./out/test/runTest.js"
  },
  "devDependencies": {
    "@types/node": "16.x",
    "@types/vscode": "^1.74.0",
    "@typescript-eslint/eslint-plugin": "^5.45.0",
    "@typescript-eslint/parser": "^5.45.0",
    "esbuild": "^0.25.11",
    "eslint": "^8.28.0",
    "typescript": "^4.9.4"
  },
  "dependencies": {
    "quill": "^1.3.7"
  }
}
